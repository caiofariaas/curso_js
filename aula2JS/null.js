let cor = null; // Não aponta para local nenhum na memória, null
let cor2;       // undefined

// diferença entre undefined e null
// undefined é quando não inicializamos uma variavel
// null é quando apontamos isso de proposito.

// Criação de um array

let a = [1, 2];
console.log(a);

// Adicionando um item a um array, Aceita strings e numeros no mesmo array

a.push("dawd", 2);
console.log(a);
